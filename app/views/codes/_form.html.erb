<%= form_with(model: code, local: true) do |form| %>
  <% if code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(code.errors.count, "error") %> prohibited this code from being saved:</h2>

      <ul>
      <% code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <%= form.label :secretcode %>
   <%= form.text_field :secretcode %>
  </div>
<br>
  <div class="field">
   <%= form.label :Number %>
   <%= form.select :desired_attribute, ['10', '20', '30', '50'] %>
  </div>
  <br>

  <div class="field">
   <%= form.label :secretcode %>
   <%= form.text_field :uuid, :value => generate_uuid %>
  </div>
  
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
